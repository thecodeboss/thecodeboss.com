{
  "name": "infra",
  "main": "index.ts",
  "scripts": {
    "deploy": "pulumi up -s prod",
    "preview": "pulumi preview -s prod --diff"
  },
  "devDependencies": {
    "@types/node": "^20.14.10"
  },
  "dependencies": {
    "@pulumi/aws": "^6.44.0",
    "@pulumi/awsx": "^2.13.0",
    "@pulumi/pulumi": "^3.124.0",
    "@pulumi/synced-folder": "^0.11.1"
  }
}
